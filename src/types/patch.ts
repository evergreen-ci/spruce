export enum MyPatchesQueryParams {
  CommitQueue = "commitQueue",
}

export enum PatchStatus {
  Created = "created",
  Started = "started",
  Success = "succeeded",
  Failed = "failed",
  All = "all",
}
