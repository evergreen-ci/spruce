mutation RestartVersions(
  $patchId: String!
  $abort: Boolean!
  $versionsToRestart: [VersionToRestart!]!
) {
  restartVersions(
    versionId: $patchId
    abort: $abort
    versionsToRestart: $versionsToRestart
  )
}
