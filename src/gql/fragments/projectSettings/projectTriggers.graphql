fragment projectTriggersSettings on Project {
  triggers {
    alias
    buildVariantRegex
    configFile
    dateCutoff
    level
    project
    status
    taskRegex
  }
}

fragment repoTriggersSettings on RepoRef {
  triggers {
    alias
    buildVariantRegex
    configFile
    dateCutoff
    level
    project
    status
    taskRegex
  }
}
