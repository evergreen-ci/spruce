fragment projectPeriodicBuildsSettings on Project {
  periodicBuilds {
    alias
    configFile
    id
    intervalHours
    message
    nextRunTime
  }
}

fragment repoPeriodicBuildsSettings on RepoRef {
  periodicBuilds {
    alias
    configFile
    id
    intervalHours
    message
    nextRunTime
  }
}
