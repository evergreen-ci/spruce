fragment projectPeriodicBuildsSettings on Project {
  periodicBuilds {
    id
    configFile
    intervalHours
    alias
    message
    nextRunTime
  }
}

fragment repoPeriodicBuildsSettings on RepoRef {
  periodicBuilds {
    id
    configFile
    intervalHours
    alias
    message
    nextRunTime
  }
}
