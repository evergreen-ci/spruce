query DistroTaskQueue($distroId: String!) {
  distroTaskQueue(distroId: $distroId) {
    buildVariant
    displayName
    expectedDuration
    id
    priority
    project
    requester
    version
  }
}
