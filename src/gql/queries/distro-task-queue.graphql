query DistroTaskQueue($distroId: String!) {
  distroTaskQueue(distroId: $distroId) {
    author
    buildVariant
    displayName
    expectedDuration
    id
    priority
    project
    requester
    version
  }
}
