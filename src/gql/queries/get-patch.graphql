#import "../fragments/basePatch.graphql"

query Patch($id: String!) {
  patch(id: $id) {
    ...basePatch
    childPatches{
      project
      patchID
      taskCount
      status  
    }
    childPatchesTemp{
      baseVersionID
      githash
      id
      projectID
      taskCount
      status
    }
    projectID
    projectIdentifier
    githash
    patchNumber
    version
    taskCount
    baseVersionID
    duration {
      makespan
      timeTaken
    }
    time {
      started
      submittedAt
      finished
    }
    canEnqueueToCommitQueue
  }
}
