query GetDisplayTask($taskId: String!, $execution: Int) {
  task(taskId: $taskId, execution: $execution) {
    id
    execution
    executionTasks
    displayTask {
      id
      execution
    }
  }
}
