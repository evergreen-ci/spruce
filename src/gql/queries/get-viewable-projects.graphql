query GetViewableProjectRefs {
  viewableProjectRefs {
    groupDisplayName
    repo {
      id
    }
    projects {
      id
      identifier
      repo
      repoRefId
      owner
      displayName
      isFavorite
      enabled
    }
  }
}
