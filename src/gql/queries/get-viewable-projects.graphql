query GetViewableProjectRefs {
  viewableProjectRefs {
    groupDisplayName
    projects {
      displayName
      enabled
      id
      identifier
      isFavorite
      owner
      repo
    }
    repo {
      id
    }
  }
}
