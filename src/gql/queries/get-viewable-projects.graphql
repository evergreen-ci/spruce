query GetViewableProjectRefs {
  viewableProjectRefs {
    groupDisplayName
    repo {
      id
    }
    projects {
      id
      identifier
      repo
      owner
      displayName
      isFavorite
      enabled
    }
  }
}
