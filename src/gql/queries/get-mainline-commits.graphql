query MainlineCommits(
  $options: MainlineCommitsOptions!
  $taskStatusCountsOptions: BuildVariantOptions!
) {
  mainlineCommits(options: $options) {
    versions {
      version {
        id
        author
        createTime
        message
        revision
        taskStatusCounts(options: $taskStatusCountsOptions) {
          status
          count
        }
      }
      rolledUpVersions {
        id
        createTime
        author
        order
        message
        revision
      }
    }
    nextPageOrderNumber
  }
}
