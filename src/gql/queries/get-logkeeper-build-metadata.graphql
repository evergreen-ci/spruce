query LogkeeperBuildMetadata($buildId: String!) {
  logkeeperBuildMetadata(buildId: $buildId) {
    id
    builder
    buildNum
    taskId
    taskExecution
    tests {
      id
      name
    }
  }
}
