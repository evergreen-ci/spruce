query LogkeeperBuildMetadata($buildId: String!) {
  logkeeperBuildMetadata(buildId: $buildId) {
    builder
    buildNum
    taskId
    taskExecution
    tests {
      buildId
      command
      id
      name
      phase
      taskExecution
      taskId
    }
  }
}
