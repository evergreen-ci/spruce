#import "../fragments/projectSettings/projectEventSettings.graphql"
query RepoEventLogs($identifier: String!) {
  repoEvents(id: $identifier) {
    count
    eventLogEntries {
      timestamp
      user
      before {
        ...repoEventSettings
      }
      after {
        ...repoEventSettings
      }
    }
  }
}
