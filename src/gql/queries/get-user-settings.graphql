query GetUserSettings {
  userSettings {
    timezone
    region
    slackUsername
    slackMemberId
    notifications {
      buildBreak
      commitQueue
      patchFinish
      patchFirstFailure
      spawnHostExpiration
      spawnHostOutcome
    }
    githubUser {
      lastKnownAs
    }
    useSpruceOptions {
      hasUsedSpruceBefore
      spruceV1
      hasUsedMainlineCommitsBefore
    }
    dateFormat
  }
}
