#import "../fragments/projectSettings/access.graphql"
#import "../fragments/projectSettings/general.graphql"
#import "../fragments/projectSettings/plugins.graphql"

query RepoSettings($repoId: String!) {
  repoSettings(id: $repoId) {
    projectRef {
      id
      ...repoGeneralSettings
      ...repoAccessSettings
      ...repoPluginsSettings
    }
  }
}
