query GetSpruceConfig {
  spruceConfig {
    banner
    bannerTheme
    jira {
      host
    }
    providers {
      aws {
        maxVolumeSizePerUser
      }
    }
    slack {
      name
    }
    spawnHost {
      spawnHostsPerUser
      unexpirableHostsPerUser
      unexpirableVolumesPerUser
    }
    ui {
      defaultProject
      userVoice
    }
  }
}
