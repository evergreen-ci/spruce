query CreatedTickets($taskId: String!) {
  bbGetCreatedTickets(taskId: $taskId) {
    fields {
      assigneeDisplayName
      created
      resolutionName
      status {
        id
        name
      }
      summary
      updated
    }
    key
  }
}
