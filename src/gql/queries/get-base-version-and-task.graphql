query GetBaseVersionAndTask($taskId: String!) {
  task(taskId: $taskId) {
    id
    execution
    displayName
    buildVariant
    versionMetadata {
      baseVersion {
        id
        order
        projectIdentifier
      }
      isPatch
    }
    baseTask {
      id
      execution
      status
    }
  }
}
