
  query GetSpawnTask($taskId: String!, $execution: Int) {
    task(taskId: $taskId, execution: $execution) {
      id
      execution
      displayName
      buildVariant
      revision
      canSync
      project {
        spawnHostScriptPath
      }
    }
  }
