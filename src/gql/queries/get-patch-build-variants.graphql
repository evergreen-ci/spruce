
  query PatchBuildVariants($patchId: String!) {
    patchBuildVariants(patchId: $patchId) {
      variant
      displayName
      tasks {
        id
        name
        status
        baseStatus
      }
    }
  }
