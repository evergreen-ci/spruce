  query Host($id: String!) {
    host(hostId: $id) {
      id
      hostUrl
      distroId
      distro {
        bootstrapMethod
      }
      tag
      provider
      startedBy
      user
      status
      runningTask {
        id
        name
      }
      lastCommunicationTime
    }
  }
