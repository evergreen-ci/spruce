#import "../fragments/baseHost.graphql"

query Host($id: String!) {
  host(hostId: $id) {
    ...baseHost
    distroId
    distro {
      id
      bootstrapMethod
    }
    runningTask {
      id
      name
    }
    ami
    lastCommunicationTime
  }
}
