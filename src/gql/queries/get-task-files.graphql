
  query TaskFiles($id: String!, $execution: Int) {
    taskFiles(taskId: $id, execution: $execution) {
      fileCount
      groupedFiles {
        taskName
        files {
          name
          link
        }
      }
    }
  }
